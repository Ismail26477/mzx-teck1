import { FileListPage } from "@/components/FileListPage";
const Reports = () => <FileListPage category="report" title="Lab Reports" emptyTitle="No reports yet" emptyHint="Add lab results and diagnostic reports to track them over time." uploadLabel="Upload Scans & Documents" pageDescription="Add lab results and diagnostic reports to track them over time." />;
export default Reports;
